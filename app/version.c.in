/* Template for version.c to be generated by CMake */
#include "version.h"

#define ATTR_BOOT __attribute__((section(".text0")))

const fw_info_t FW_Info ATTR_BOOT = {
    .magic = {'T', 'P', 'F', 'W'},
    .vendor = FW_Info_Vendor,
    .product = FW_Info_Product,
    .version = FW_Info_Version,
    .build_date = FW_Info_BuildDate,
    .flags = 0x40000000U,
    .data = NULL
};

/* Version constants for console output */
const char FW_Info_Vendor[] ATTR_BOOT = "Toppoint e.V.";
const char FW_Info_Homepage[] ATTR_BOOT = "https://toppoint.de/";
const char FW_Info_Product[] ATTR_BOOT = "RGB Stripe";
const char FW_Info_BuildDate[] ATTR_BOOT = "${BUILDDATE}";
const char FW_Info_Version[] ATTR_BOOT = "${GITVERSION}";
